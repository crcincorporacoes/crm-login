# Sistema de Login para Dashboard de CRM — Plano de Implementação

> **Para executores automatizados:** SUB-SKILL OBRIGATÓRIA: use superpowers:subagent-driven-development (recomendado) ou superpowers:executing-plans para implementar este plano tarefa por tarefa. Os passos usam a sintaxe de checkbox (`- [ ]`) para rastreamento.

**Objetivo:** Construir um sistema de login com Next.js + Supabase que protege o acesso a um dashboard placeholder de CRM, incluindo recuperação/redefinição de senha.

**Arquitetura:** Next.js 15 (App Router) + TypeScript, com Supabase Auth para autenticação por email/senha. O `@supabase/ssr` gerencia a sessão via cookies tanto no servidor quanto no cliente. Um `middleware.ts` na raiz renova a sessão em cada requisição e redireciona usuários não autenticados que tentem acessar `/dashboard`. As ações de login, logout e senha são Server Actions do Next.js — sem rotas de API separadas.

**Stack Tecnológica:** Next.js 15, React 18, TypeScript, `@supabase/supabase-js`, `@supabase/ssr`, `tsx` (para rodar o script de criação de usuário via linha de comando).

**Spec:** `docs/superpowers/specs/2026-09-15-login-crm-design.md`

## Restrições Globais

- Sem autocadastro público. Contas são criadas manualmente via painel do Supabase ou `scripts/create-user.ts` — nunca exponha uma rota `/signup`.
- Toda autenticação/sessão passa pelo `@supabase/ssr` (`createBrowserClient` / `createServerClient`) — nunca implemente lógica de cookie/sessão na mão.
- Ações de login/logout/senha são Server Actions, não rotas de API.
- Mensagens de erro mostradas ao usuário são genéricas e nunca revelam se um email existe no sistema (ex.: "Email ou senha inválidos", nunca "usuário não encontrado").
- Segredos ficam em `.env.local` (fora do git); `.env.example` é versionado com valores vazios como modelo.
- Sem suíte de testes automatizados (decisão explícita da spec — desproporcional para este escopo). Cada tarefa termina com um passo de **verificação manual**: rodar `npm run dev` e checar o comportamento no navegador. Não pule esses passos nem os substitua por testes automatizados.
- Não há Row Level Security (RLS) neste projeto — não existem tabelas customizadas no Postgres ainda (só `auth.users`, gerenciada pelo Supabase e não exposta via API pública). Quando o CRM ganhar tabelas de dados reais em um projeto futuro, RLS passa a ser obrigatório nelas — ver spec, seção "Segurança de dados (RLS)".
- Todo texto de interface (UI) é em português (pt-BR).

---

### Tarefa 1: Estruturar o projeto Next.js

**Arquivos:**
- Criar: `package.json`, `tsconfig.json`, `next.config.ts`, `next-env.d.ts`, `.gitignore`, `.eslintrc.json`, `app/layout.tsx`, `app/globals.css`, `app/page.tsx` (placeholder temporário, substituído na Tarefa 4)

**Interfaces:**
- Produz: um servidor Next.js funcional (`npm run dev`) e a pasta `app/` na qual todas as tarefas seguintes adicionam rotas.

- [ ] **Passo 1: Rodar o create-next-app na raiz do projeto**

A raiz do projeto hoje contém apenas `.git/` e `docs/`, ambos tratados como seguros pelo `create-next-app` — ele monta a estrutura ao redor deles sem apagar nada.

Rodar:
```bash
npx --yes create-next-app@latest . --typescript --eslint --app --no-tailwind --no-src-dir --import-alias "@/*" --use-npm
```

Se aparecer algum prompt interativo, responda: TypeScript = Yes, ESLint = Yes, Tailwind = No, diretório `src/` = No, App Router = Yes, import alias `@/*` = Yes.

- [ ] **Passo 2: Verificar que o servidor de desenvolvimento roda**

Rodar: `npm run dev`
Esperado: o servidor sobe em `http://localhost:3000` e a página padrão de boas-vindas do Next.js carrega no navegador sem erros. Pare o servidor (Ctrl+C) depois de confirmar.

- [ ] **Passo 3: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js project"
```

---

### Tarefa 2: Dependências do Supabase, configuração de ambiente e clientes

**Arquivos:**
- Modificar: `package.json` (adicionar dependências)
- Criar: `.env.example`
- Criar: `lib/supabase/client.ts`
- Criar: `lib/supabase/server.ts`

**Interfaces:**
- Consome: nada das tarefas anteriores.
- Produz:
  - `createClient()` de `lib/supabase/client.ts` — retorna um cliente Supabase de navegador, para uso em Client Components.
  - `createClient()` (assíncrona) de `lib/supabase/server.ts` — retorna `Promise<SupabaseClient>`, para uso em Server Components e Server Actions. Toda tarefa seguinte que fala com o Supabase no servidor chama `await createClient()` deste arquivo.

- [ ] **Passo 1: Instalar os pacotes do Supabase**

Rodar:
```bash
npm install @supabase/supabase-js @supabase/ssr
npm install -D tsx
```

- [ ] **Passo 2: Criar o `.env.example`**

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

- [ ] **Passo 3: Criar o seu `.env.local` real**

Copie `.env.example` para `.env.local` (já ignorado pelo git pelo scaffold do Next.js). Vá no painel do seu projeto Supabase → Project Settings → API, e preencha:
- `NEXT_PUBLIC_SUPABASE_URL` — a Project URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` — a chave `anon` `public`
- `SUPABASE_SERVICE_ROLE_KEY` — a chave `service_role` (mantenha em segredo, só usada pelo script de CLI da Tarefa 9)
- `NEXT_PUBLIC_SITE_URL` — deixe como `http://localhost:3000` para desenvolvimento local

Se você ainda não tem um projeto Supabase, crie um gratuitamente em supabase.com antes de continuar — todas as tarefas seguintes precisam de credenciais reais para verificação.

- [ ] **Passo 4: Criar o cliente Supabase de navegador**

Criar `lib/supabase/client.ts`:

```ts
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

- [ ] **Passo 5: Criar o cliente Supabase de servidor**

Criar `lib/supabase/server.ts`:

```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Chamado durante a renderização de um Server Component — pode
            // ser ignorado, o middleware renova a sessão na próxima requisição.
          }
        },
      },
    }
  )
}
```

- [ ] **Passo 6: Verificação manual**

Rodar: `npm run build`
Esperado: build conclui sem erros de TypeScript (os arquivos ainda não são usados em lugar nenhum, mas precisam compilar corretamente).

- [ ] **Passo 7: Commit**

```bash
git add package.json package-lock.json .env.example lib/supabase/client.ts lib/supabase/server.ts
git commit -m "feat: add Supabase client helpers and env config"
```

---

### Tarefa 3: Middleware para renovação de sessão e proteção de rotas

**Arquivos:**
- Criar: `lib/supabase/middleware.ts`
- Criar: `middleware.ts`

**Interfaces:**
- Consome: variáveis de ambiente `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` (Tarefa 2).
- Produz: `updateSession(request)` de `lib/supabase/middleware.ts`, usada apenas por `middleware.ts`. Garante que qualquer requisição a `/dashboard` sem sessão válida seja redirecionada para `/login` antes da página renderizar.

- [ ] **Passo 1: Criar o helper de renovação de sessão**

Criar `lib/supabase/middleware.ts`:

```ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  const {
    data: { user },
  } = await supabase.auth.getUser()

  const isDashboardRoute = request.nextUrl.pathname.startsWith('/dashboard')

  if (isDashboardRoute && !user) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}
```

Este arquivo cria seu próprio `createServerClient` (em vez de usar o `createClient()` da Tarefa 2) porque o middleware lê/escreve cookies através de `NextRequest`/`NextResponse`, não através do `cookies()` do `next/headers` — os dois rodam em contextos diferentes.

- [ ] **Passo 2: Conectar o middleware raiz**

Criar `middleware.ts` (raiz do projeto, ao lado de `package.json`):

```ts
import { type NextRequest } from 'next/server'
import { updateSession } from '@/lib/supabase/middleware'

export async function middleware(request: NextRequest) {
  return await updateSession(request)
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

- [ ] **Passo 3: Verificação manual**

Rodar: `npm run dev`, depois visitar `http://localhost:3000/dashboard` no navegador (a página ainda não existe — sem problema).
Esperado: você é redirecionado para `/login` (uma página 404, já que `/login` também ainda não existe — mas a URL na barra de endereço precisa mudar para `/login`, provando que o redirecionamento do middleware disparou).

- [ ] **Passo 4: Commit**

```bash
git add middleware.ts lib/supabase/middleware.ts
git commit -m "feat: add auth middleware to protect /dashboard routes"
```

---

### Tarefa 4: Página de redirecionamento raiz

**Arquivos:**
- Modificar: `app/page.tsx` (substitui o placeholder da Tarefa 1)

**Interfaces:**
- Consome: `createClient()` de `lib/supabase/server.ts` (Tarefa 2).
- Produz: nada consumido por tarefas seguintes — é uma rota folha.

- [ ] **Passo 1: Substituir a home page por um redirecionamento consciente de autenticação**

Substituir o conteúdo de `app/page.tsx`:

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export default async function RootPage() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  redirect(user ? '/dashboard' : '/login')
}
```

- [ ] **Passo 2: Verificação manual**

Rodar: `npm run dev`, visitar `http://localhost:3000/`.
Esperado: redirecionado para `/login` (ainda 404 por enquanto, mas a barra de endereço mostra `/login` — confirma que a lógica roda).

- [ ] **Passo 3: Commit**

```bash
git add app/page.tsx
git commit -m "feat: redirect root page based on auth state"
```

---

### Tarefa 5: Página de login e Server Action

**Arquivos:**
- Criar: `app/login/page.tsx`
- Criar: `app/login/actions.ts`

**Interfaces:**
- Consome: `createClient()` de `lib/supabase/server.ts` (Tarefa 2).
- Produz: a rota `/login` para a qual as Tarefas 3, 4, 6, 7, 8 redirecionam em caso de falha de autenticação.

- [ ] **Passo 1: Criar a Server Action de login**

Criar `app/login/actions.ts`:

```ts
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function login(formData: FormData) {
  const email = formData.get('email') as string
  const password = formData.get('password') as string

  const supabase = await createClient()
  const { error } = await supabase.auth.signInWithPassword({ email, password })

  if (error) {
    redirect(`/login?error=${encodeURIComponent('Email ou senha inválidos')}`)
  }

  redirect('/dashboard')
}
```

- [ ] **Passo 2: Criar a página de login**

Criar `app/login/page.tsx`:

```tsx
import { login } from './actions'

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>
}) {
  const { error } = await searchParams

  return (
    <main style={{ maxWidth: 360, margin: '80px auto', fontFamily: 'sans-serif' }}>
      <h1>Entrar</h1>
      <form action={login} style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
        <label>
          Email
          <input name="email" type="email" required style={{ display: 'block', width: '100%' }} />
        </label>
        <label>
          Senha
          <input name="password" type="password" required style={{ display: 'block', width: '100%' }} />
        </label>
        {error && <p style={{ color: 'red' }}>{error}</p>}
        <button type="submit">Entrar</button>
      </form>
      <p>
        <a href="/forgot-password">Esqueci minha senha</a>
      </p>
    </main>
  )
}
```

- [ ] **Passo 3: Criar um usuário de teste no Supabase**

No painel do Supabase → Authentication → Users → Add user, crie um usuário com um email real que você controle e uma senha, marcando "Auto Confirm User" (assim você não precisa configurar envio de email ainda para testar o login).

- [ ] **Passo 4: Verificação manual**

Rodar: `npm run dev`, visitar `http://localhost:3000/login`.
- Enviar com senha errada → esperado: redirecionado de volta para `/login?error=...`, a página mostra "Email ou senha inválidos".
- Enviar com as credenciais reais do usuário de teste → esperado: redirecionado para `/dashboard` (404 por enquanto — sem problema, confirma que o login funcionou).

- [ ] **Passo 5: Commit**

```bash
git add app/login
git commit -m "feat: add login page and sign-in server action"
```

---

### Tarefa 6: Página do dashboard e logout

**Arquivos:**
- Criar: `app/dashboard/page.tsx`
- Criar: `app/dashboard/actions.ts`

**Interfaces:**
- Consome: `createClient()` de `lib/supabase/server.ts` (Tarefa 2); depende do middleware da Tarefa 3 para já garantir que existe sessão.
- Produz: a rota `/dashboard`.

- [ ] **Passo 1: Criar a Server Action de logout**

Criar `app/dashboard/actions.ts`:

```ts
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function logout() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  redirect('/login')
}
```

- [ ] **Passo 2: Criar a página do dashboard**

Criar `app/dashboard/page.tsx`:

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { logout } from './actions'

export default async function DashboardPage() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  return (
    <main style={{ maxWidth: 480, margin: '80px auto', fontFamily: 'sans-serif' }}>
      <h1>Bem-vindo, {user.email}</h1>
      <form action={logout}>
        <button type="submit">Sair</button>
      </form>
    </main>
  )
}
```

O `redirect('/login')` aqui é uma proteção extra — o middleware (Tarefa 3) já bloqueia requisições não autenticadas antes deste componente renderizar.

- [ ] **Passo 3: Verificação manual**

Rodar: `npm run dev`.
- Fazer login com o usuário de teste em `/login` → esperado: redirecionado para `/dashboard`, a página mostra "Bem-vindo, {seu email}".
- Clicar em "Sair" → esperado: redirecionado para `/login`.
- Depois de sair, tentar visitar `/dashboard` diretamente → esperado: redirecionado de volta para `/login` (o middleware bloqueia).

- [ ] **Passo 4: Commit**

```bash
git add app/dashboard
git commit -m "feat: add protected dashboard page with logout"
```

---

### Tarefa 7: Fluxo de "esqueci minha senha"

**Arquivos:**
- Criar: `app/forgot-password/page.tsx`
- Criar: `app/forgot-password/actions.ts`

**Interfaces:**
- Consome: `createClient()` de `lib/supabase/server.ts` (Tarefa 2); variável de ambiente `NEXT_PUBLIC_SITE_URL` (Tarefa 2).
- Produz: a rota `/forgot-password`, referenciada a partir de `/login` (Tarefa 5). Envia o usuário para `/auth/callback?next=/reset-password`, construída na Tarefa 8.

- [ ] **Passo 1: Criar a Server Action de solicitação de redefinição de senha**

Criar `app/forgot-password/actions.ts`:

```ts
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function requestPasswordReset(formData: FormData) {
  const email = formData.get('email') as string
  const supabase = await createClient()

  const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'

  const { error } = await supabase.auth.resetPasswordForEmail(email, {
    redirectTo: `${siteUrl}/auth/callback?next=/reset-password`,
  })

  if (error) {
    redirect(`/forgot-password?error=${encodeURIComponent('Não foi possível enviar o email, tente novamente')}`)
  }

  redirect('/forgot-password?sent=1')
}
```

- [ ] **Passo 2: Criar a página de "esqueci minha senha"**

Criar `app/forgot-password/page.tsx`:

```tsx
import { requestPasswordReset } from './actions'

export default async function ForgotPasswordPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string; sent?: string }>
}) {
  const { error, sent } = await searchParams

  return (
    <main style={{ maxWidth: 360, margin: '80px auto', fontFamily: 'sans-serif' }}>
      <h1>Esqueci minha senha</h1>
      {sent ? (
        <p>Se esse email existir, enviamos um link de redefinição de senha.</p>
      ) : (
        <form action={requestPasswordReset} style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
          <label>
            Email
            <input name="email" type="email" required style={{ display: 'block', width: '100%' }} />
          </label>
          {error && <p style={{ color: 'red' }}>{error}</p>}
          <button type="submit">Enviar link</button>
        </form>
      )}
      <p>
        <a href="/login">Voltar ao login</a>
      </p>
    </main>
  )
}
```

- [ ] **Passo 3: Verificação manual**

Rodar: `npm run dev`, visitar `http://localhost:3000/forgot-password`, enviar o email do usuário de teste.
Esperado: a página mostra a mensagem de confirmação "se esse email existir...". Confira no painel do Supabase → Authentication → Logs (ou na sua caixa de email, se o SMTP estiver configurado) que um email de redefinição foi disparado. Não tem problema se o link dentro dele der 404 por enquanto — a Tarefa 8 constrói a página para a qual ele aponta.

- [ ] **Passo 4: Commit**

```bash
git add app/forgot-password
git commit -m "feat: add forgot-password page and reset-request action"
```

---

### Tarefa 8: Fluxo de redefinição de senha (callback de autenticação + página de reset)

**Arquivos:**
- Criar: `app/auth/callback/route.ts`
- Criar: `app/reset-password/page.tsx`
- Criar: `app/reset-password/actions.ts`

**Interfaces:**
- Consome: `createClient()` de `lib/supabase/server.ts` (Tarefa 2); acessada via o link gerado na Tarefa 7.
- Produz: as rotas `/auth/callback` e `/reset-password`. É a última peça do fluxo de autenticação — nenhuma tarefa seguinte depende dela.

- [ ] **Passo 1: Criar a rota de callback de autenticação**

Criar `app/auth/callback/route.ts`:

```ts
import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/dashboard'

  if (code) {
    const supabase = await createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) {
      return NextResponse.redirect(`${origin}${next}`)
    }
  }

  return NextResponse.redirect(`${origin}/login?error=${encodeURIComponent('Link inválido ou expirado')}`)
}
```

- [ ] **Passo 2: Criar a Server Action de atualização de senha**

Criar `app/reset-password/actions.ts`:

```ts
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function updatePassword(formData: FormData) {
  const password = formData.get('password') as string
  const supabase = await createClient()

  const { error } = await supabase.auth.updateUser({ password })

  if (error) {
    redirect(`/reset-password?error=${encodeURIComponent('Não foi possível salvar a nova senha')}`)
  }

  redirect('/login')
}
```

- [ ] **Passo 3: Criar a página de redefinição de senha**

Criar `app/reset-password/page.tsx`:

```tsx
import { updatePassword } from './actions'

export default async function ResetPasswordPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>
}) {
  const { error } = await searchParams

  return (
    <main style={{ maxWidth: 360, margin: '80px auto', fontFamily: 'sans-serif' }}>
      <h1>Nova senha</h1>
      <form action={updatePassword} style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
        <label>
          Nova senha
          <input name="password" type="password" required minLength={6} style={{ display: 'block', width: '100%' }} />
        </label>
        {error && <p style={{ color: 'red' }}>{error}</p>}
        <button type="submit">Salvar nova senha</button>
      </form>
    </main>
  )
}
```

- [ ] **Passo 4: Verificação manual (fluxo completo de redefinição)**

Rodar: `npm run dev`.
- Ir em `/forgot-password`, enviar o email do usuário de teste.
- Abrir o email de redefinição (checar sua caixa de entrada, ou copiar o link dos Auth Logs do Supabase se o SMTP não estiver configurado) e clicar/colar o link.
- Esperado: você cai em `/reset-password`.
- Enviar uma nova senha → esperado: redirecionado para `/login`.
- Fazer login com a nova senha → esperado: redirecionado para `/dashboard` com sucesso.

- [ ] **Passo 5: Commit**

```bash
git add app/auth app/reset-password
git commit -m "feat: add auth callback and reset-password page"
```

---

### Tarefa 9: Script de criação de usuário via CLI, README e verificação final

**Arquivos:**
- Criar: `scripts/create-user.ts`
- Criar: `README.md`

**Interfaces:**
- Consome: variáveis de ambiente `NEXT_PUBLIC_SUPABASE_URL` e `SUPABASE_SERVICE_ROLE_KEY` (Tarefa 2).
- Produz: nada consumido em outro lugar — é a última tarefa.

- [ ] **Passo 1: Criar o script de CLI**

Criar `scripts/create-user.ts`:

```ts
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY

if (!supabaseUrl || !serviceRoleKey) {
  console.error('Defina NEXT_PUBLIC_SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY no .env.local')
  process.exit(1)
}

const [, , email, password] = process.argv

if (!email || !password) {
  console.error('Uso: npx tsx scripts/create-user.ts <email> <senha>')
  process.exit(1)
}

const supabase = createClient(supabaseUrl, serviceRoleKey)

async function main() {
  const { data, error } = await supabase.auth.admin.createUser({
    email,
    password,
    email_confirm: true,
  })

  if (error) {
    console.error('Erro ao criar usuário:', error.message)
    process.exit(1)
  }

  console.log('Usuário criado:', data.user?.email)
}

main()
```

- [ ] **Passo 2: Verificação manual do script**

Rodar:
```bash
npx tsx --env-file=.env.local scripts/create-user.ts outro-teste@example.com senhaSegura123
```
Esperado: o console mostra `Usuário criado: outro-teste@example.com`. Confirme no painel do Supabase → Authentication → Users que o novo usuário aparece. Depois faça login com ele em `/login` para confirmar que funciona de ponta a ponta.

- [ ] **Passo 3: Escrever o README**

Criar `README.md`:

```markdown
# CRM Login

Sistema de login (email/senha) protegendo o acesso a um dashboard de CRM.
Next.js 15 + TypeScript + Supabase Auth.

## Configuração

1. `npm install`
2. Crie um projeto no [Supabase](https://supabase.com) (gratuito).
3. Copie `.env.example` para `.env.local` e preencha com as chaves do seu
   projeto Supabase (Project Settings → API).
4. `npm run dev` e acesse `http://localhost:3000`.

## Criando usuários

Não há autocadastro. Crie contas de duas formas:

- Painel do Supabase → Authentication → Users → Add user.
- Linha de comando:
  ```bash
  npx tsx --env-file=.env.local scripts/create-user.ts email@exemplo.com senha123
  ```

## Rotas

- `/login` — login
- `/forgot-password` — solicitar redefinição de senha
- `/reset-password` — definir nova senha (via link do email)
- `/dashboard` — área protegida (redireciona para `/login` sem sessão)

## Segurança de dados (RLS)

Este projeto não cria tabelas customizadas no Postgres — apenas usa
`auth.users`, gerenciada internamente pelo Supabase. Ao adicionar tabelas
reais do CRM (contatos, negócios, etc.) em uma feature futura, ative Row
Level Security em cada uma delas antes de expor dados.
```

- [ ] **Passo 4: Passada final de verificação manual completa**

Rodar: `npm run dev` e percorrer, em ordem:
1. Visitar `/` deslogado → redirecionado para `/login`.
2. Senha errada em `/login` → mensagem de erro mostrada, permanece em `/login`.
3. Senha correta em `/login` → redirecionado para `/dashboard`, mostra seu email.
4. Visitar `/dashboard` diretamente em uma janela anônima/privada nova (sem sessão) → redirecionado para `/login`.
5. Fazer login de novo, clicar em "Sair" → redirecionado para `/login`, e `/dashboard` deixa de ser acessível sem logar de novo.
6. O fluxo completo de esqueci/redefinir senha (como na Tarefa 8, Passo 4) continua funcionando.

Esperado: cada passo se comporta como descrito, sem erros no console do navegador ou do terminal.

- [ ] **Passo 5: Commit**

```bash
git add scripts README.md
git commit -m "feat: add user-creation CLI script and README"
```
